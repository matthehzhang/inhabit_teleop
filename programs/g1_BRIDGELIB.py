#!/usr/bin/env python3 

#i think this makes it ^^ so when it runs it runs in python3?
# (check this, this statement may not be factual)

import time 
import math
import sys #read sys arg type things, such as num of args (used for sim or real identification/categorization)

#rewrite the lib matthew zhang haichengman
